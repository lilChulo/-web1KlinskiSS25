export class User {
    constructor(userId, password, firstName, lastName) {
        this.userId = userId;
        this.password = password;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}
