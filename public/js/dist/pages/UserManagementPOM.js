import { AbstractPOM } from './AbstractPOM.js';
export class UserManagementPOM extends AbstractPOM {
    constructor(appManager) {
        super(appManager);
    }
    showPage() {
        // Platzhalter für den nächsten Meilenstein
    }
}
